# Session 2026-07-10 — Work day 2: log-driven fixes + the 17-item list

Logs: data/7-10-2026/ (15MB from the work machine). Headlines from evidence:
2,106 files failed pydantic validation (0 successful saves all day); the
new guard blocked a 349-file all-new folder (this was the LTS3 folder — it
never reached the parser, hence "wouldn't process at all"); the drift tab
render threw AttributeError on every model; the 170k-file scan ran minutes
with no feedback ("takes a long time before anything happens").

## P0 (commit 413167e, pushed 2026-07-10 evening)
1. pydantic ≥2.12 rejects NaN on constrained fields → BaseAnalysisModel
   coerces NaN→None model-wide (NaN = not measured). Version-proof.
2. Guard v2: fires only when ≥50 files with KNOWN filenames fail
   recognition; all-new folders (daily batches, LTS3) sail through.
3. Drift tab: _MetricRow used self.theme (never assigned) in TWO places;
   the app sweep now constructs the tab against real drift state — the
   new check caught the second call site immediately.
4. Predictor panel: shows accuracy/F1/AUC, training count, top signals.

## P1/P2 (this commit) — James's 17-item list
- #2 wheel steps prev/next model on the selector (dropdown can't scroll in CTk)
- #3 "Final Test Units" tab: FT records w/ result, date, linked, match %
- #4 "Export all charts" (Units tab): every unit in the window → print PNGs
- #5 Triage cards: current-production models first, then |shift|
- #6 export dates are day strings (no 00:00:00)
- #7 Unit history: linearity pass/status/FT-result cells color-filled
- #8/#16 evidence pack gains "Final test units" + "Smoothness" sheets
- #9 History tab figure enlarged (11×6.8)
- #10 per-model ML retrain shows live progress (N/M — model, phase)
- #11 "Found N new files (M already in database)" always shown; plus
  immediate "Scanning folder…" and a heartbeat every 2,000 files during
  the database check (the silent minutes were read as a lockup)
- #14 Smoothness tab lists which models HAVE smoothness data
- #15 header button renamed "Export model to Excel"
- #12/#13 were P0 (drift tab, predictor)
- #17 (the daily question: holding or drifting, historically difficult?)
  is served by: pills+drift tab (state), History/Monthly (direction),
  Trim-vs-FT + FT Units (difficulty), triage ordering (where to look).

Sweep: 48 checks, 0 FAIL, 1 WARN (known mislabeled 8074 FT file).

## Full-log error taxonomy (user challenge: "you went through ALL the logs?")
Clustered every ERROR line across the four log files (~7,600 lines):
| Count | Signature | Disposition |
|---|---|---|
| 2,106 | TrackData validation (NaN, pydantic 2.12) | FIXED (P0) — will process correctly on retry |
| 2,035 | FT "Serial cannot be empty" | 2016 distinct files, mostly 2013–2017 'Resistance Output' shop logs with NO serial in the filename — permanently unprocessable → now recorded as skipped after first attempt |
| 839 | "Excel file format cannot be determined" | 2011-era 6607 files in pre-2003/corrupt format → permanent skip |
| 137 | "Model cannot be empty" | naming anomalies, no model resolvable → permanent skip |
| ~188 | FT Format-1 float errors ('Writer_Version', 'Record Length'…) | oscilloscope captures in the Test Station tree → land as serial-empty → permanent skip |
| 36 | UNIQUE constraint (final_test_results) | duplicates of already-saved records → permanent skip |
| 22 | drift tab render AttributeError | FIXED (P0) |
| 2 | xlrd "directory corruption" | corrupt OLE containers → permanent skip |
| 1 | guard v1 abort ("matched 0 of 349") | the LTS3 folder — FIXED (guard v2) |
| 1 | file literally named ".xls" | permanent skip |

New mechanism: Processor._is_permanent_failure — known-permanent signatures
are recorded as skipped (with stat) after the FIRST attempt, so ~4,000 junk
files stop re-erroring on every scan. Content change re-triggers a parse;
full reprocess (incremental off) retries everything after parser upgrades.
Transient errors (network, memory) still retry. Test coverage in
tests/test_scan_path_rescue.py.
