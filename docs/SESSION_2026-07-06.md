# Session 2026-07-06 — Full V6 review + P0/P1 fix sweep

Full-code review of V5+V6 (4 parallel deep reviews: docs/history, v6 GUI, core+database, ML), test run, live-DB inspection. Findings and remaining roadmap in `docs/V6_FINISH_PLAN_2026-07-06.md`. Then fixed everything in P0/P1 plus the work-blocking processing hang. All changes uncommitted — review and commit.

## Fixed this session

**1. Processing hang at "checking against database" (work-blocking).**
`_is_processed` hashed (fully read) every path-hit file — every re-scan of a mostly-processed network folder read every byte of every known file, in 8 KB chunks.
- `utils/hashing.py`, `core/parser.py:_calculate_hash`: 8 KB → 1 MB chunks.
- `core/processor.py`: stat fast-path — path-hit + recorded size/mtime match (2s tolerance) skips with a single `stat()` call, no read. Hash-confirm remains the fallback (content stays the identity); confirmed rows self-heal their stat record (`database/manager.py:update_processed_file_stats`) so the next scan is fast. DB verified: 92,149/92,149 rows already carry size+mtime.
- Tests: `tests/test_incremental_stat_fastpath.py` (6).

**2. Freeze mechanism (P1) — no more cross-thread Tk, no more main-thread DB.**
- New `gui/v6/ui_dispatch.py`: workers enqueue onto a plain queue; the root drains it on the main loop (33 ms). `PageBase.safe_after` reworked; clones in training_modal + all six settings sections routed through it. `active_models.py` also read Tk widgets from the worker — values now captured on the main thread first.
- Main-thread DB calls moved to workers: `UnitChartModal` track fetch (modal opens instantly with "Loading…"), per-model-specs load/save/delete + startup combobox fill, Model-page copy-summary (~10 agg queries), PredictorPanel `load_all` (unpickles every model), V6App startup auto-train gate.

**3. Model page silent blanking (P1).**
- `smoothness_tab.py:37`: sort crashed on None file_date (datetime vs int) → `datetime.min`.
- `model_page._reload`: one broad try/except silently zeroed every tab. Now per-loader AND per-widget guards; failures logged per tab, other tabs unaffected.

**4. Drift engine wiring (P0) — Triage now responds to new data.**
- `advance_drift_state` had ZERO callers (state frozen at training, 2026-06-24). Now called per-model after every Process batch (`process_page._run`) and once at app startup (catch-up, 5 s delay).
- Same-day boundary bug: `file_date > last_updated` skipped same-day arrivals forever (day-granularity dates). New `last_row_id` watermark column (autoincrement source-row id: track_results.id / smoothness_results.id) + startup migration; date filter kept only as one-shot fallback for pre-migration rows.
- Tests: `tests/test_drift_advance_same_day.py` (3).

**5. Preset consistency (P0).**
`preview_alert_count` + `apply_sensitivity_preset` skipped the Bonferroni /9 correction training applies — "Save preset" wrote ~9× looser thresholds than a retrain; preview lied. Shared helper `drift_training.corrected_tier_thresholds` now used by all three paths. Bonus: apply no longer stomps `last_updated` (it's the advance marker, not an audit field).
- Tests: `tests/test_preset_bonferroni_consistency.py` (2).

**6. Drift-state fidelity (P0).**
- `recent_window` persisted (new SafeJSON column + migration): hydrated detectors woke with empty windows — STEP_CHANGE was unreachable at read time and the σ-shift worst-metric tiebreaker was always 0.
- Composite demotion: keyed on `is_trained` alone, a degenerate or stale composite silenced the whole trim-effort family. Now requires non-degenerate baseline + freshness (staleness = composite's sample marker >30 days behind siblings → `represents_family=False` at hydrate).
- Zero-mean degenerate hole: training's std=1e-9 sentinel passed the CV guard for zero-mean constants → absolute floor added to `is_degenerate_baseline`.
- Tests: `tests/test_drift_state_fidelity_2026_07_06.py` (7).

**7. 3rd-laser guard.**
- Unknown trim layouts no longer silently parse under System B's column map — `_detect_system_from_sheets` raises (per-file ERROR result, batch continues).
- `detect_file_type` burial of unrecognized structures now logs at WARNING with an explicit third-system hint (they're marked skipped — "Reset skipped files" in Settings reprocesses them once System C support lands). Full System C support still needs a sample file.
- Tests: `tests/test_unknown_system_guard.py` (4).

**8. Housekeeping.** Removed stale `.git/index.lock` from a June 24 git crash (it caused the phantom staged-deletions of active_models.py/history_tab.py) and restored the index.

**9. Evidence pack v2 — the "export a model's full history to Excel" workflow.**
James' actual workflow (his words): analyze units → export the model to Excel to read full history → judge whether the process is moving for better or worse → unit charts for the team. The old pack was a 365-day window with 7 columns.
- `export/evidence.py`: three sheets now — **Drift evidence** (adds honest "Shift (σ)" matching the UI; the old `Delta_sigma` column exported the alert-scaled magnitude, which contradicted the screen — same fix applied to Copy-summary text), **Unit history** (FULL record by default, one row per unit/track, all watched metrics + trim-pass count + composite + pass flags + system), **Monthly summary** (units, pass/warn/fail, pass rate, metric means per month — the better-or-worse read at a glance).
- Model page export button now always exports the full record (the on-screen window stays a view control).
- Unit charts for the team: already in v6 — unit chart modal → "Save chart…" (PNG/PDF/SVG).
- Tests: `tests/test_evidence_pack_full_history.py` (2).

**10. Company Trends section on the Dashboard (v5 Trends' surviving job).**
James: v6 had no company-as-a-whole view. Rather than porting the 4k-line v5 Trends page (its other jobs live in Triage/Model now), the Dashboard gained a Company Trend section:
- `database/manager.py:get_company_yield_trend(days_back, period)` — company pass-rate per week/month PLUS per-system (A/B/C) split in one grouped query. UNTRIMMED excluded from numerator and denominator (dashboard convention; the older `get_yield_trend` counts them — left untouched for v5).
- `gui/v6/widgets/company_trend_chart.py` — company line (accent, bold) over per-system overlays (A green / B purple / C amber) with a units-volume bar backdrop; ~10 x-ticks max; y-window fits the informative band.
- Dashboard: "Company trend" section between the yield panels and worst-models list, Weekly/Monthly toggle, honors the page window selector (30d/90d/365d/All), loads in the existing worker, failure can't blank the rest of the page.
- The per-system split doubles as the LTS3 ramp monitor — System C's line vs A/B answers "is the new laser trending healthy."
- Tests: `tests/test_company_yield_trend.py` (2).

**11. Full 3rd-laser (System C / LTS3) support — unblocked.**
James: LTS3's files are sheet-identical to an existing system and live under a parent folder named `LTS3`. Design: **sheets decide FORMAT** (A-style or B-style extraction, per file — no need to know which family LTS3 copies), **path decides SYSTEM** (any directory segment starting with `LTS3`, case-insensitive, / or \\ separators → tagged C).
- `core/models.py` + `database/models.py`: `SystemType.C` added to both enums (DB column is plain VARCHAR(1), no CHECK constraint — verified, no rebuild needed).
- `core/parser.py`: `parse_file` split into `format_type` (drives extraction, incl. the 8340 dual-spec gate) vs `system_type` (goes into metadata); `_resolve_system_identity` does the path check (filename excluded — only directories match).
- `database/manager.py`: save/load/update mappings handle C; `get_system_comparison` returns a `system_c` bucket.
- Startup migration retags already-processed LTS3 rows (stored as A/B before path detection existed) → C, in analysis_results + final_test_results. Idempotent; on this repo's DB copy it's a no-op (0 LTS3 paths), it will act on the work DB.
- v6 GUI is system-agnostic (verified) — C flows through everything incl. drift/Triage. v5 dashboard's A-vs-B panel ignores C (no crash; v5 is being retired). Data note: 8340-1 is System B (2,452 rows) — so "the laser that trims 8340-1" = System B in app terms.
- Tests: `tests/test_system_c_lts3.py` (6).

**12. Chart-quality + data-integrity pass (2026-07-07 evening, after design review).**
- **Windows anchored to data, not wall clock:** Model page cutoff (focus/units/smoothness) anchors to the model's latest file_date; `get_model_trim_ft_agreement` / `get_model_measurement_history` accept `cutoff_date`. Fixes alert-lands-on-empty-chart for stale models.
- **Company trend honesty:** partial week/month drawn hollow + "partial" note (calendar-aware rule), "Data through YYYY-MM-DD" vintage caption, legend moved above the axes (was on the data), duplicate title dropped, last x-tick always shown.
- **Cosmetics:** composite pill label shortened (no more truncation), History tab panel-title collision fixed (h_pad), unit-chart legend deterministic lower-right w/ translucent frame (was loc='best' parking on traces).
- **Unit modal parity with v5:** now passes optimal_offset (corrected trace), trim_improvement_percent, and trim date to the chart.
- **Outlier-gated recent means (M2):** `compute_recent_means` excludes values >8 baseline-σ (only against trained, non-degenerate baselines) and discloses exclusions (`with_meta`). Copy-summary and evidence pack show "N suspect values excluded" / "Suspect excluded" column. Real-data effect: model 8887's headline shift +16.32σ → −0.17σ with 2 exclusions disclosed.
- **Ingest scale-anomaly guard (M3):** `_validate_track_data` Check 5 flags linearity errors >10× the file's own spec band as suspect data_quality (the 10.007-vs-±0.05 production case).
- **WARNING status recompute (M4):** `db.recompute_overall_statuses(dry_run)` re-grades overall_status from stored track flags under the current rule (NULL-flag rows skipped for Fix Missing Tracks; ERROR/UNTRIMMED untouched; batched; idempotent). Settings → Database Cleanup → "Recompute unit statuses" with preview-then-confirm. **Dry run on this DB: 7,840 of 78,326 would change — ALL WARNING→FAIL — 5,379 skipped (NULL flags). Not executed; run from Settings.**
- Tests: `tests/test_status_recompute.py` (2). Full suite re-run green.

**13. Yield semantics corrected to the customer basis (James' domain rule, 2026-07-07).**
Zero tolerance applies to LINEARITY only (customer requirement); sigma is an internal drift-watch metric, never a disposition. The status rule already encoded this (sigma only separates PASS from WARNING) — but every yield HEADLINE counted clean-PASS only, presenting an internal process-watch number as yield (Dashboard read "59.8% pass" when customer-acceptable yield was 94.1%).
- `core/yield_stats.compute_yield`: adds `linearity_yield` = (PASS+WARNING)/gradeable; trend carries both rates; `worst_models_by_yield` trim% now customer basis.
- YieldPanel headline: "X% linearity yield"; breakdown relabeled Pass / **Watch** / Fail; sparkline follows the headline basis.
- Company trend: numerator = linearity-accepted; fields renamed `accepted`/`linearity_yield`; y-axis "linearity yield %". All-time view now correctly shows 80–100% (May 2026: 93.4%) instead of 40–70% clean-pass noise.
- Evidence pack monthly: both columns, labeled — "Linearity yield %" (customer) and "Clean pass %" (internal); Warning column renamed "Watch (sigma)".
- NOTE: until the status recompute (item 12) is executed, WARNING still hides 7,840 linearity-FAILs, so linearity-yield headlines OVERSTATE until that backfill runs — run "Recompute unit statuses" first for exact numbers.
- Tests updated: company trend (WARNING-in-numerator case), evidence pack columns; full suite green.

**14. Autonomous chart-QA sweep (2026-07-07 late) — permanent harness + fixes.**
`scripts/chart_qa_render_all.py`: renders EVERY v6 chart against a real-data variant matrix (dense/sparse/single/stale models, fail-heavy + multi-track units, smoothness, 4 trend windows) into `docs/v6_design_review_2026-07-07/qa_sweep/` with a manifest — run after any chart change. Defects it caught and their fixes:
- Daily-median line spiked full-height on majority-corrupt batch days → off-window medians become line GAPS (red markers + disclosure tell that day's story).
- Sparse/no-baseline mode: outliers stretched the y-window AND off-window points vanished silently (929Ω hidden among 4,700s) → robust 10–90/family window in both modes; clamp + disclose is now universal (amber without baseline, red = Rule-1 with).
- **Fail-point/verdict contradiction (correctness):** modal computed fail X's on RAW errors while grading used the CORRECTED trace — exports showed "Fail Points: 3" beside "Linearity Pass: YES" (unit 8074-1/30). `compute_fail_points` now takes the stored offset.
- **Detector diet winsorization:** training replay and advance now CLIP samples to baseline±8σ (shared `SUSPECT_SIGMA_GATE` in drift_types) — isolated scale corruption gets one bounded CUSUM push and decays; sustained real shifts still trip in ~2 samples. 8887-class fake OOC tiers self-correct on next retrain.
- Cosmetics: volume-axis label ghosting, "(units)" label suffix.
- Data observations for follow-up: 6607 has a 2.1e8 Ω resistance row and multi-modal electrical-angle conventions across years (angle drift baselines unreliable for such models); historical suspect-row backfill should accompany Fix Missing Tracks.

## After pulling these changes on the work machine
1. First launch runs two automatic migrations (`last_row_id`, `recent_window` columns).
2. Run Settings → Retrain drift detector once — populates watermarks and persisted windows immediately (otherwise they fill on first advance).
3. Re-run the folder that hung at "checking against database" — expect minutes → seconds after the first (self-healing) pass.

## Still open (see V6_FINISH_PLAN_2026-07-06.md)
- WARNING status recompute backfill (42% of rows, mixed meanings — AS9100-relevant)
- Fix Missing Tracks can't see NULL-array rows; FT-derived rows fabricate sigma values
- ~~Full 3rd-laser (System C) support~~ **done this session** (path-based; sanity-check one LTS3 file's sheet tabs on the work machine)
- V5→V6 feature gap review — James flagged v5 features he still wants; scope TBD
- Ingest-time spec discrepancy check; Triage N+1 batching; evidence-pack "Δσ" label mismatch; History-tab per-track vs per-unit rate
- v5 page retirement + old single-metric drift detector removal; main→V6 merge audit
